{
  "name": "atrisbridge",
  "private": true,
  "version": "0.1.0-alpha.9",
  "type": "module",
  "scripts": {
    "dev": "vite --port 1420",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "sidecar:prepare": "node scripts/prepare-rclone.mjs",
    "mcp:prepare": "node scripts/prepare-mcp-companion.mjs",
    "mcp:verify": "node scripts/verify-mcp-companion.mjs",
    "sidecar:prepare:release": "node scripts/prepare-release-rclone.mjs && node scripts/prepare-mcp-companion.mjs",
    "sidecar:verify:release": "node scripts/verify-release-rclone.mjs && node scripts/verify-mcp-companion.mjs",
    "icons:prepare": "tauri icon public/brand/atrisbridge-mark.svg",
    "test:release-contract": "node --test .github/scripts/release-contract.test.mjs",
    "tauri": "tauri",
    "tauri:dev": "npm run mcp:prepare && npm run icons:prepare && tauri dev",
    "tauri:build": "npm run sidecar:prepare:release && npm run icons:prepare && node .github/scripts/create-package-build-config.mjs src-tauri/tauri.package.conf.json && tauri build --config src-tauri/tauri.package.conf.json"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0",
    "lucide-react": "^0.468.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  }
}