# AtrisBridge product UI

This module owns the desktop product shell, product views, state model, and UI design-system layers. Existing synchronization engines and transport components remain outside this module and are composed here without duplicating their business logic.
